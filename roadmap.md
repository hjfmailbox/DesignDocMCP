# Dev-Protocol Onboarding Roadmap

## D0: Environment Preparation

### D0.1 Cleanup
Remove protocol test pollution and stale onboarding state.
Preserve v2 runtime integration in `.claude/skills/`.

### D0.2 Real /dev-init
First real execution of `/dev-init` on DesignDocMCP.
Validate state generation and project rule extraction.

### D0.3 First /dev-scope
Execute `/dev-scope` to establish initial development scope.
Capture goal decomposition behavior.

### D0.4 First /dev-save + /dev-status Loop
Run `/dev-save` to persist checkpoint.
Run `/dev-status` to verify state recovery.

### D0.5 Friction Collection
Document real-world friction points during v2 runtime usage.
Feed findings into dev-protocol backlog (separate from DesignDocMCP backlog).

---

*This roadmap tracks the dev-protocol v2 runtime onboarding process for DesignDocMCP.*
