# project-rules.md
# Auto-generated by /dev-bootstrap on 2026-05-23

## 编码规范

- Python 项目，使用 `pyproject.toml` 管理依赖
- 源码位于 `src/designdoc_mcp/`
- 测试位于 `tests/`
- 文档位于 `docs/`

## 架构约束

- **单例模式**: 全局单例已去重，web.py 委托 server.py
- **存储双后端**: SQLite (sqlite_store.py) + 文件锁 (store.py)
- **事件溯源**: 所有操作记录为事件，完整追溯讨论过程
- **MCP 协议**: Streamable HTTP 模式，所有 Agent 连接同一实例

## 安全规范

- API Token 认证机制已启用
- 文件锁使用 O_CREAT + O_EXCL 原子创建
- 不得在提交中包含敏感文件 (.env, credentials 等)

## 开发工作流

- 使用 `/dev-checkpoint` 建立检查点基线
- 使用 `/dev-resume` 恢复开发上下文
- 协议文件位于 `.agents/dev-protocol/`
- 不得自行创建或更新根目录下的兼容文件副本

## 用户偏好

- 与用户交互使用中文（专业名词除外）
- 只连接已运行的 MCP 服务器，不自行启动（来自用户记忆）
- 使用 RTK (Rust Token Killer) 优化 token 使用

## 测试规范

- 使用 pytest 进行测试
- 测试缓存位于 `.pytest_cache/`
